/* eslint-env jest */
// Set a longer timeout for performance tests
// Jest option 'testTimeout' at project config caused warnings, so we set it here
jest.setTimeout(30000);

// Optionally, we can tweak gc or other perf-related hooks here if needed in future
